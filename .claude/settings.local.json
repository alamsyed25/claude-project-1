{
  "permissions": {
    "allow": [
      "Bash(cmd.exe /c \"where node && where npm && where npx\")",
      "Bash(export PATH=\"/c/Program Files/nodejs:$PATH\")",
      "Bash(npm run build:*)"
    ]
  }
}
