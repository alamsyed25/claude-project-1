export const SUPPORTED_FILE_TYPES = ['.txt', '.md', '.docx', '.pdf'] as const

export const MAX_FILE_SIZE_MB = 10

export const MAX_FILE_SIZE_BYTES = MAX_FILE_SIZE_MB * 1024 * 1024

export const APP_NAME = 'CompareDoc'
